---
name: "tagged-release"

# yamllint disable-line rule:truthy
on:
  push:
    tags:
      - "*"

permissions:
  attestations: write
  id-token: write
  contents: write

jobs:
  tagged-release:
    name: tagged-release
    runs-on: ubuntu-24.04
    env:
      CI_BRANCH: release
      PACKAGECLOUD_REPOSITORY: dokku/dokku
      VERSION: ${{ github.ref_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: "go.mod"

      - name: Get Repository Name
        id: repo-name
        run: |
          echo "REPOSITORY_NAME=$(echo "${{ github.repository }}" | cut -d '/' -f 2)" >> $GITHUB_OUTPUT
          echo "GO_VERSION=$(go mod edit -json | jq -r .Go)" >> $GITHUB_OUTPUT

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          working-directory: .github

      - name: Build
        run: bundle exec make build
        env:
          BUNDLE_GEMFILE: .github/Gemfile

      - name: Stage release artifacts
        run: |
          mkdir -p dist

          # copy build/$PLATFORM/coredns-docker-$ARCH to dist/coredns-docker-$PLATFORM-$ARCH
          for platform in darwin linux windows; do
            for arch in amd64 arm64 arm mips mips64le ppc64le riscv64 s390x; do
              if [ -f build/$platform/coredns-docker-$arch ]; then
                cp build/$platform/coredns-docker-$arch dist/coredns-docker-$platform-$arch
              fi
            done
          done

          cp build/windows/coredns-docker-amd64.exe dist/coredns-docker-windows-amd64.exe
          cp build/deb/*.deb dist/

      - name: Attest Build Provenance - darwin-amd64
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-darwin-amd64
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-darwin-arm64
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-amd64
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-arm
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-arm64
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-mips
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-mips64le
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-ppc64le
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-riscv64
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-linux-s390x
            dist/${{ steps.repo-name.outputs.REPOSITORY_NAME }}-windows-amd64.exe"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/*

      - name: Release to PackageCloud
        run: bundle exec make release-packagecloud
        env:
          BUNDLE_GEMFILE: .github/Gemfile
          PACKAGECLOUD_TOKEN: ${{ secrets.PACKAGECLOUD_TOKEN }}

      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          generate_release_notes: true
          make_latest: "true"
